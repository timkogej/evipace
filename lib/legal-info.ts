/**
 * Populate once a real Privacy Notice page exists and is approved. Until
 * then this stays null and the send-request form shows a concise, true,
 * generic notice without a clickable link — never a fake/placeholder
 * Privacy Policy page. Per explicit instruction, the submission feature
 * itself is not exposed publicly until this is set and the page is real.
 */
export const privacyPolicyPath: string | null = null;
